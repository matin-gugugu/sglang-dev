# Phase45控制端执行提示

在精确W45的干净本地CPU worktree中，以受保护Phase15 raw的绝对路径运行preflight、run、verify。不得使用GPU、联网、训练、生成Hfull或读取Phase43标签。结果只允许包含300行target-free特征、300行低维画像、600行冻结预测和审计文件；完整请求与raw不得进入Git。R45必须是W45的唯一子提交，验收后才能ff-only合入并制作Phase46。
