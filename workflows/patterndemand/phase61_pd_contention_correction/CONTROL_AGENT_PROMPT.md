# Phase61控制端执行提示

在exact W61上本地CPU执行本目录preflight.py、run.py和verify.py。禁止GPU、网络、新物理测量及任何reserved future blind标签访问。

只允许Phase60的120个development official point参与候选拟合。选择必须由leave-one-payload-pair-out OOF机械决定，并优先选择第一个达标的最简单候选。回传W61/R61、运行时间、baseline与修正后OOF WAPE、六个“配置×拓扑”切片、最终公式、输入边界、manifest/DONE及可得和不可得结论。

Phase61通过只代表development公式已冻结；不得把它表述为fresh blind通过。下一步另写Phase62 GPU workflow。
