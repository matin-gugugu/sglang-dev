# Phase 14E Decode schedule analysis

This analysis reconstructs active-batch sequences from configured per-request output lengths and tests compact schedule features on top of the backend-free Phase 14D models. Actual backend, kernel names, model identity, and measured target values are never predictive features.
