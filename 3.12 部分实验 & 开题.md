#### 3.12 部分实验 & 开题

1. 3* 3 *3 workload （repeat * 3）

   - prefill：处理输入 prompt 的阶段
   - decode：逐 token 生成输出的阶段

   这两个阶段在 benchmark 执行时显式分开: 在 [bench_one_batch.py](https://vscode-remote+attached-002dcontainer-002b7b22636f6e7461696e65724e616d65223a222f73676c616e672d646966667573696f6e2d636868227d-0040ssh-002dremote-002bradixark02.vscode-resource.vscode-cdn.net/root/.vscode-server/extensions/openai.chatgpt-26.304.20706-linux-x64/webview/#) 中，prefill 通过 profile_context("prefill", reqs) 包裹 extend(...) 调用，只执行一次整段输入处理；decode 则通过 profile_context("decode", reqs) 包裹 decode(...) 调用，在逐 token 循环中重复执行。

   在 TP 通信原语所在的 [parallel_state.py](https://vscode-remote+attached-002dcontainer-002b7b22636f6e7461696e65724e616d65223a222f73676c616e672d646966667573696f6e2d636868227d-0040ssh-002dremote-002bradixark02.vscode-resource.vscode-cdn.net/root/.vscode-server/extensions/openai.chatgpt-26.304.20706-linux-x64/webview/#) 中，对 all_reduce、all_gather 等 collective 通信进行埋点统计。每当发生一次通信，就根据当前所处阶段分别累计其通信字节数和调用次数，得到四个阶段化原始指标：

   - prefill_comm_bytes_total
   - prefill_comm_calls_total
   - decode_comm_bytes_total
   - decode_comm_calls_total

   同时，benchmark 记录两个阶段的时间指标：

   - prefill_latency

   - median_decode_latency

     > 当前阶段 = prefill，发生一次 all_reduce, 往 prefill 标签下的统计里加 bytes 和 calls
     >
     > 当前阶段 = decode，发生一次 all_gather，往 decode 标签下的统计里加 bytes 和 calls

   其中 prefill_latency 表示整段输入在 prefill 阶段的耗时，median_decode_latency 表示 decode 过程中单 token 延迟的中位数。

   原始 benchmark 完成后，[phase0_experiment.py](https://vscode-remote+attached-002dcontainer-002b7b22636f6e7461696e65724e616d65223a222f73676c616e672d646966667573696f6e2d636868227d-0040ssh-002dremote-002bradixark02.vscode-resource.vscode-cdn.net/root/.vscode-server/extensions/openai.chatgpt-26.304.20706-linux-x64/webview/#) 使用 aggregate-demand 对同一 workload 的多次重复实验进行聚合。对于时延指标，脚本按 workload 分组后计算平均值和标准差，得到：

   - prefill.latency_ms_avg
   - prefill.latency_ms_std
   - decode.latency_per_output_token_ms_avg
   - decode.latency_per_output_token_ms_std

   对于通信指标，脚本进一步做 token 归一化。具体来说：

   - 用 prefill_comm_bytes_total / prompt_len 得到 prefill.bytes_per_prompt_token
   - 用 prefill_comm_calls_total / prompt_len 得到 prefill.calls_per_prompt_token
   - 用 decode_comm_bytes_total / max_token 得到 decode.bytes_per_output_token
   - 用 decode_comm_calls_total / max_token 得到 decode.calls_per_output_token

   因此，最终每个 workload 点都可以同时得到两类阶段化指标：

   1. 时延类指标
      - prefill 平均时延
      - decode 单 token 平均时延
   2. 通信类指标
      - prefill 每输入 token 通信字节数
      - prefill 每输入 token 通信调用次数
      - decode 每输出 token 通信字节数
      - decode 每输出 token 通信调用次数

   先在 benchmark 执行时按 prefill/decode 两阶段分别记时并分别统计 TP 通信，再在聚合阶段对原始结果做平均和 per-token 归一化，最终得到用于 PatternDemand 的阶段化性能指标。

**1. bench_one_batch.py 和 sglang.launch_server **

[bench_one_batch.py](https://vscode-remote+attached-002dcontainer-002b7b22636f6e7461696e65724e616d65223a222f73676c616e672d646966667573696f6e2d636868227d-0040ssh-002dremote-002bradixark02.vscode-resource.vscode-cdn.net/root/.vscode-server/extensions/openai.chatgpt-26.304.20706-linux-x64/webview/#) 是离线 benchmark 脚本。不启动 HTTP 服务，不接收外部请求，在单个进程内部直接加载模型，构造固定 workload，执行一次标准化推理，然后输出 prefill 和 decode 的原始性能指标。测到的结果更接近模型执行本体，包括算子执行、TP 通信以及阶段本身的时延。

sglang.launch_server 是服务启动入口。把模型作为一个常驻推理服务跑起来，对外提供请求处理能力。服务模式下，请求需要经过完整系统链路，包括网络收发、请求排队、调度、动态批处理、缓存管理以及多请求并发干扰等。因此它测到的不是“模型本体执行下界”，而是“完整服务系统中的端到端表现”。

**第一阶段的目标**抽取模型本身的 PatternDemand，而不是直接测真实服务延迟：

- prefill 阶段本身要花多少时间
- decode 阶段每个 token 要花多少时间
- TP 通信需要多少字节
- TP 通信要发生多少次

把推理系统和网络拓扑的噪声剥离掉，只保留和模型执行机制本身有关的 demand。避免一开始就用 server 形态，测量结果混入：

- HTTP 开销
- scheduler 行为
- 动态 batching
- 队列等待
- 系统后台线程扰动

第一阶段使用 bench_one_batch.py，不经过服务框架的情况下，直接测到模型开销指标。

第二阶段用 launch_server 测量真实系统与拓扑带来的附加 Cost：

- 链路带宽相关代价
- 链路 RTT / 同步相关代价
- 服务调度与系统组织带来的附加时延

对任意 workload：

- `comm_time_est = bytes_per_prompt_token / bw_eff + calls_per_output_token × rtt_eff`



1. 要以此类推考虑PP 和 PD分离的情况。
2. 专利。
3. 